name: Run CI unittests
on:
  pull_request:
    types: [opened, ready_for_review, synchronize]
  workflow_dispatch:

jobs:
  run-tests:
    uses: ./.github/workflows/ci-matrix-gen.yml
    with:
      jobs_to_run: nodejs-weekly-es-module-jdklatest-linux-aarch64 #(?!.*(aux-engine|smoke|pgo|bench|graalvm|style))